{
  "name": "gayaseva-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "GayaSeva Platform Monorepo",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "postinstall": "npm install --prefix backend && npm run build --prefix backend",
    "build": "npm run build --prefix backend",
    "start": "npm start --prefix backend",
    "build:website": "npm install --prefix frontend/website && npm run build --prefix frontend/website",
    "start:website": "npm start --prefix frontend/website",
    "dev": "concurrently -n \"WEBSITE,ADMIN\" -c \"bgYellow.bold,bgBlue.bold\" \"npm run dev --prefix frontend/website\" \"npm run dev --prefix frontend/admin\"",
    "dev:website": "npm run dev --prefix frontend/website",
    "dev:admin": "npm run dev --prefix frontend/admin",
    "test:backend": "npm run test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
